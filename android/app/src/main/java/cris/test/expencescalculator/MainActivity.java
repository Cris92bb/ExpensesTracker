package cris.test.expencescalculator;

import android.graphics.Color;
import android.os.Build;
import android.os.Bundle;
// It's generally good practice to remove unused imports,
// but android.view.Window might be used later for status bar customization with V2 embedding if needed.
// For now, let's comment it out if no other direct use is present.
// import android.view.Window; 

import io.flutter.embedding.android.FlutterActivity;
// GeneratedPluginRegistrant is not needed for V2 embedding.

public class MainActivity extends FlutterActivity {
  // onCreate is not typically needed for basic V2 embedding unless
  // you have specific logic to add here. For now, it can be removed or left empty.
  // @Override
  // protected void onCreate(Bundle savedInstanceState) {
  //   super.onCreate(savedInstanceState);
  //   // GeneratedPluginRegistrant.registerWith(this) is no longer needed.
  // }
}
